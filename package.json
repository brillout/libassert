{
  "name": "@brillout/libassert",
  "version": "0.1.1",
  "description": "Assertions for library authors.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "npm run build:dev",
    "build:dev": "tsc --build --watch",
    "build:prod": "tsc --build",
    "clean": "git clean -Xdf",
    "prepublishOnly": "npm run clean && npm install && npm run build:prod"
  },
  "devDependencies": {
    "@types/node": "^14.11.2",
    "typescript": "^4.0.3"
  },
  "repository": "github:brillout/libassert",
  "publishConfig": {
    "access": "public"
  }
}
